import 'package:flutter/material.dart';

class MonitoringAndroidStatusPage extends StatelessWidget {
  const MonitoringAndroidStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
